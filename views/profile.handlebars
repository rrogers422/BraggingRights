<section class="container">
  {{!-- <h3 class="profile">{{profile.name}}</h3> --}}
 <a href = "/active"><h4>Active Bets: {{numActiveBets}}</h4></a>

  <h4>Wins: {{userWins}}</h4>
  <h4>Losses: {{userLosses}}</h4>
  <section class="flex-row justify-center">
    {{!-- {{#each  as ||}}
    {{> }}
    {{/each}} --}}
  </section>
  <a href = "/add"><h5>Create a bet!</h5></a>
</section>
<script src="./js/logout.js"></script>
